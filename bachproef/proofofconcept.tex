%%=============================================================================
%% Proof of Concept
%%=============================================================================

\chapter{Proof of Concept}%
\label{ch:proofofconcept}

De proof of concept bevat een gedetailleerde bespreking van de implementatie van het model, inclusief de vergelijking tussen de verschillende versies van de code, van high-level tot low-level en \glsxtrshort{tflite}. Het doel is om de effectiviteit en efficiëntie van \gls{robbert} op verschillende platformen te evalueren en om de capaciteiten van een niet-gefinetunde \gls{robbert} te concluderen. In elke integratie die volgt, zijn de volgende modellen getest of gebruikt:

\begin{itemize}
    \item DTAI-KULeuven/robbertje-1-gb-non-shuffled5
    \item pdelobelle/robbert-v2-dutch-base
    \item DTAI-KULeuven/robbert-2022-dutch-base
    \item DTAI-KULeuven/robbert-2023-dutch-base
    \item DTAI-KULeuven/robbert-2023-dutch-large
\end{itemize}

\section{Python Implementatie}

Alle code die in deze sectie wordt besproken, is beschikbaar in de volgende GitHub-repository: \url{https://github.com/Chmpy/RobBERT_local_example}. Deze repository bevat de volledige implementatie van de scripts en configuraties die nodig zijn om de \glsxtrshort{tflite}-modellen te verkrijgen en gebruiken

\subsection{Vereisten en Setup}

In deze sectie worden de vereiste bibliotheken en hun versies besproken voor de implementatie van de proof of concept in Python. Daarnaast beschrijven we het utility-bestand dat zorgt voor consistent gedrag tussen de scripts.

Voor het uitvoeren van de scripts en het installeren van de benodigde bibliotheken wordt Python versie \texttt{3.11.5} gebruikt. Het is van cruciaal belang om deze versie te hanteren om ervoor te zorgen dat de scripts correct functioneren. De vereiste bibliotheken zijn gedefinieerd in het \texttt{requirements.txt}-bestand, zoals weergegeven in codefragment \ref{lst:requirements}. 

\begin{listing} [ht]
    \begin{minted}{text}
        # requirements.txt
        transformers~=4.41.2
        tensorflow~=2.16.2
        torch~=2.3.1
        optimum~=1.20.0
        numpy~=1.26.4
    \end{minted}
    \caption[\texttt{requirements.txt}]{Het \texttt{requirements.txt}-bestand specificeert de versies van bibliotheken die nodig zijn om de scripts te draaien en zorgt voor de juiste compatibiliteit tussen de verschillende Python-pakketten.}
    \label{lst:requirements}
\end{listing}

Het utility-bestand bevat functies die zorgen voor consistent gedrag tussen de verschillende scripts. Deze functies omvatten:

\subsubsection{Voorbeeldzinnen}
De functie \texttt{get\_sentences} levert vooraf bepaalde zinnen aan elk script, zoals weergegeven in codefragment \ref{lst:getsentences}. Deze zinnen worden gebruikt voor het testen van de modellen en demonstreren correcte en incorrecte grammaticale structuren met betrekking tot de woorden "die" en "dat".

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # utils.py
        def get_sentences():
            """Get example sentences."""
            return [
                "Ik heb een vriend die altijd te laat komt.",  # Correct
                "Ik weet die ik het kan.",  # Incorrect
                "Ik weet dat ik het kan.",  # Correct
                "Daarom is het belangrijk, je moet goed opletten.",  # Incorrect
                "Ik ken een man die altijd grapjes maakt.",  # Correct
                "Ze heeft een jurk gekocht die perfect past.",  # Correct
                "Er is een boek dat ik je echt kan aanraden.",  # Correct
                "We bezochten een stad die bekend staat om haar architectuur.",  # Correct
                "Hij las een artikel dat zijn mening veranderde.",  # Correct
                "Ze vertelde over een ervaring die haar leven veranderde.",  # Correct
                "Ik zag een film die mij aan het denken zette.",  # Correct
                "Hij gebruikt een methode die zeer effectief is.",  # Correct
                "Het kind dat in de tuin speelt, is mijn neefje.",  # Correct
                "De vraag die ze stelde, was erg moeilijk.",  # Correct
                "De beslissing dat hij maakte, was erg moeilijk.",  # Incorrect
                "Dit is de laptop die ik wil kopen.",  # Correct
                "Dit is de laptop dat ik wil kopen.",  # Incorrect
                "Het project dat we gestart zijn, verloopt goed.",  # Correct
                "De vrouw die je daar ziet, is mijn lerares.",  # Correct
                "De resultaten dat we behaalden, waren indrukwekkend.",  # Incorrect
                "De resultaten die we behaalden, waren indrukwekkend.",  # Correct
                "De bloemen die je hebt meegenomen, zijn prachtig.",  # Correct
                "De informatie dat hij gaf, was nuttig.",  # Incorrect
                "De informatie die hij gaf, was nuttig.",  # Correct
                "Het idee dat ze voorstelde, was briljant.",  # Correct
                "Het idee die ze voorstelde, was briljant.",  # Incorrect
                "De kat dat op het dak zit, is van ons.",  # Incorrect
                "De kat die op het dak zit, is van ons.",  # Correct
                "De film die we gisteren keken, was spannend.",  # Correct
                "De auto dat ik gisteren zag, was heel mooi.",  # Incorrect
            ]
    \end{minted} 
    \caption[\texttt{get\_sentences}]{\texttt{get\_sentences}: Deze functie levert een lijst van voorbeeldzinnen die correct en incorrect gebruik van "die" en "dat" illustreren. Deze zinnen worden gebruikt voor validatie en testing binnen de proof of concept.}
    \label{lst:getsentences}
\end{listing}

\subsubsection{Preprocessing}
De functie \texttt{preprocess\_input} voert uniforme preprocessing uit voor het genereren van een maskeringstoken binnen een zin, zoals getoond in codefragment \ref{lst:preprocessinput}. Deze functie identificeert de positie van de maskeringstoken en vervangt "die" of "dat" door "<mask>", wat essentieel is voor de verwerking door het model.

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # utils.py
        def preprocess_input(sentence):
            """
            Mask 'die' or 'dat' in the input sentence.
            
            Args:
            sentence (str): Input sentence.
            
            Returns:
            tuple: (preprocessed sentence, mask position or None)
            """
            words = sentence.split()
            mask_position = None
            for i, word in enumerate(words):
                if word.lower() in ['die', 'dat']:
                    words[i] = '<mask>'
                    mask_position = i
                    break
            return ' '.join(words), mask_position
    \end{minted}
    \caption[\texttt{preprocess\_input}]{\texttt{preprocess\_input}: Deze functie voert preprocessing uit op de invoerzinnen door "die" of "dat" te vervangen met "<mask>". Het retourneert zowel de bewerkte zin als de positie van de maskeringstoken.}
    \label{lst:preprocessinput}
\end{listing}

\subsubsection{Top Voorspellingen}
De functie \texttt{get\_top\_predictions} haalt de top voorspellingen op door \gls{softmax} toe te passen op de \glspl{logit} van het model, zoals weergegeven in codefragment \ref{lst:gettopk}. Dit zorgt voor de selectie van de beste voorspellingen op basis van de maskeringstoken en geeft de top \(k\) voorspelde woorden terug. De \glspl{logit} worden eerst omgezet in \glspl{tensor} voordat \gls{softmax} wordt toegepast, om de waarschijnlijkheden van voorspellingen te verkrijgen.

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # utils.py
        def get_top_predictions(logits, vocab, mask_position, top_k=5):
            """
            Get top-k predictions for the masked token.
            
            Args:
            logits (numpy.ndarray or torch.Tensor): Model output logits
            vocab (dict): Vocabulary dictionary
            mask_position (int): Position of the mask token
            top_k (int): Number of top predictions to return
            
            Returns:
            list: Top-k predicted words
            """
            # Convert logits to tensor if it's not already
            if not isinstance(logits, (tf.Tensor, torch.Tensor)):
                logits = tf.convert_to_tensor(logits)
            
            # Get logits for the mask token
            mask_token_logits = logits[0, mask_position + 1]
            
            # Apply softmax to calculate probabilities
            if isinstance(logits, torch.Tensor):
                probs = torch.nn.functional.softmax(mask_token_logits, dim=0)
                top_indices = torch.topk(probs, k=top_k).indices.tolist()
            else:
                probs = tf.nn.softmax(mask_token_logits)
                top_indices = tf.math.top_k(probs, k=top_k).indices.numpy()
            
            # Convert indices to words
            return [list(vocab.keys())[list(vocab.values()).index(idx)] for idx in top_indices]
    \end{minted}
    \caption[\texttt{get\_top\_predictions}]{\texttt{get\_top\_predictions}: Deze functie zet de logit-uitvoer van het model om in \glspl{tensor} en past vervolgens \gls{softmax} toe om de waarschijnlijkheden van voorspellingen te berekenen. Het retourneert de top \(k\) voorspellingen op basis van de maskeringstoken en het vocabulaire.}
    \label{lst:gettopk}
\end{listing}

\subsection{Reproductie met High-Level Pipeline}

In deze fase bespreken we het eerste script waarin \gls{robbert} wordt gebruikt. Zoals eerder vermeld, maken we hierbij gebruik van de high-level \glsxtrshort{api} van \gls{huggingface}, genaamd pipelines. Dit script vormde de basis voor verdere ontwikkeling en testing.

Het script \texttt{main.py}, zoals weergegeven in de codefragmenten \ref{lst:main-part1} en \ref{lst:main-part2}, voert \glsxtrshort{mlm} uit met behulp van de Transformers pipeline op verschillende \gls{robbert}-modellen. Het script maakt gebruik van de \texttt{pipeline} functionaliteit van \gls{huggingface} om modellen eenvoudig te laden en inferentie uit te voeren. Dit vormt een efficiënte manier om gemaskeerde woorden te voorspellen in vooraf bepaalde zinnen.

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # main.py
        import logging
        import timeit
        from transformers import pipeline
        from utils import preprocess_input, get_sentences
        
        # Set up logging
        logging.basicConfig(level=logging.INFO)
        
        def set_log_level(log_level):
            """Set the logging level."""
            logging.getLogger().setLevel(log_level)
        
        def run_inference(sentence, pipe):
            """Process sentence with mask, predict masked word."""
            if "<mask>" not in sentence:
                logging.debug("No mask token found in the sentence.")
                return
            return pipe(sentence)[0]
            
        def main(model_path):
            """Main function to process sentences with the specified model."""
            
            # Load the model
            pipe = pipeline("fill-mask", model=model_path)
            logging.info(f"Model loaded: {model_path}")
            
            # Example sentences
            sentences = get_sentences()
            
            start_total_time = timeit.default_timer()
            for sentence in sentences:
                masked_sentence, _ = preprocess_input(sentence)
                start_time = timeit.default_timer()
                output = run_inference(masked_sentence, pipe)
                elapsed_time = timeit.default_timer() - start_time
                logging.info(f"Original sentence: {sentence}")
                logging.info(f"Processed sentence: {masked_sentence}")
                logging.info(f"Prediction: {output}")
                logging.info(f"Elapsed time: {elapsed_time:.3f} seconds\n")
            
            total_time = timeit.default_timer() - start_total_time
            logging.info(f"Total elapsed time: {total_time:.3f} seconds")
            logging.info("Average time per sentence: {:.3f} seconds".format(total_time / len(sentences)))
    \end{minted}
    \caption[\texttt{main.py} (deel 1)]{Het eerste deel van \texttt{main.py} definieert de functies die nodig zijn voor het uitvoeren van \glsxtrshort{mlm}. Het script laadt modellen en verwerkt zinnen met behulp van de \texttt{pipeline} functionaliteit van \gls{huggingface}. De belangrijkste functies zijn \texttt{run\_inference}, die voorspellingen doet voor gemaskeerde woorden, en \texttt{main}, die de zinnen ophaalt, modellen laadt, en de totale verwerkingstijd registreert.}
    \label{lst:main-part1}
\end{listing}

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # main.py
        if __name__ == '__main__':
            """
            This script performs masked language modeling using the Transformers pipeline
            on various Dutch BERT models. It provides a simple way to load and run inference
            using the high-level pipeline API.
            
            Key features:
            1. Uses Transformers pipeline for easy model loading and inference
            2. Supports multiple Dutch BERT models
            3. Preprocesses input sentences to include a mask token
            4. Runs inference to predict masked words
            5. Provides logging of original sentences, processed sentences, and predictions
            
            Usage:
            Run the script to process a set of predefined sentences with different Dutch
            BERT models using the Transformers pipeline.
            
            Models used:
            - DTAI-KULeuven/robbertje-1-gb-non-shuffled5
            - pdelobelle/robbert-v2-dutch-base
            - DTAI-KULeuven/robbert-2022-dutch-base
            - DTAI-KULeuven/robbert-2023-dutch-base
            - DTAI-KULeuven/robbert-2023-dutch-large
            """
            
            # Uncomment to set debug logging
            # set_log_level(logging.DEBUG)
            
            # Process sentences with different models
            main("DTAI-KULeuven/robbertje-1-gb-non-shuffled")
            main("pdelobelle/robbert-v2-dutch-base")
            main("DTAI-KULeuven/robbert-2022-dutch-base")
            main("DTAI-KULeuven/robbert-2023-dutch-base")
            main("DTAI-KULeuven/robbert-2023-dutch-large")
    \end{minted}
    \caption[\texttt{main.py} (deel 2)]{Het tweede deel van \texttt{main.py} voert de hoofdtoepassing uit, waarbij gebruik wordt gemaakt van een lijst met vooraf gedefinieerde modellen. Dit gedeelte initialiseert het script door verschillende \gls{robbert}-modellen te testen en de inferentie uit te voeren op de vooraf bepaalde zinnen. Het documenteert de kernfuncties en gebruiksinstructies in de \texttt{docstring} voor verduidelijking en instructies.}
    \label{lst:main-part2}
\end{listing}

Het script begint met het instellen van logging via de \texttt{logging} module om inzicht te geven in de verwerkingsstappen en resultaten. De functie \texttt{set\_log\_level} maakt het mogelijk om het logniveau aan te passen, wat nuttig is voor het controleren van gedetailleerde uitvoer tijdens het debuggen.

De kern van het script is de \texttt{main} functie, die verantwoordelijk is voor het verwerken van zinnen met een gespecificeerd model. Binnen deze functie wordt eerst het model geladen met behulp van de \texttt{pipeline} \glsxtrshort{api} van \gls{huggingface}, wat het eenvoudig maakt om verschillende modellen te gebruiken door enkel het pad naar het model te specificeren.

Vervolgens haalt de functie \texttt{get\_sentences} een lijst op van voorbeeldzinnen die zullen worden gebruikt voor inferentie. Elke zin wordt voorbewerkt met de \texttt{preprocess\_input} functie, die het masker token in de zin plaatst en de positie ervan identificeert. Dit is cruciaal voor \gls{robbert} om de juiste voorspellingen te kunnen maken.

De functie \texttt{run\_inference} neemt de gemaskeerde zin en het geladen model om het gemaskeerde woord te voorspellen. Het controleert of er een \texttt{<mask>} token aanwezig is; zo niet, dan wordt een debugbericht gelogd en wordt de functie beëindigd zonder inferentie. Voor zinnen met een \texttt{<mask>} token retourneert het de topvoorspelling.

Het script meet de tijd die nodig is om elke zin te verwerken, evenals de totale verwerkingstijd voor alle zinnen. Dit gebeurt met de \texttt{timeit} module, die helpt bij het vastleggen van prestatiemetrics die nuttig zijn voor analyse.

Tot slot biedt het script flexibiliteit door meerdere modellen te ondersteunen. Dit maakt het mogelijk om de prestaties van verschillende modellen te vergelijken en te testen binnen dezelfde structuur. De uitvoer wordt gelogd, inclusief de originele en gemaskeerde zinnen, de voorspelde woorden en de benodigde tijd voor elke inferentie.

Dit script is een essentieel onderdeel van het proof of concept, aangezien het de mogelijkheden van \gls{robbert}-modellen demonstreert met behulp van de high-level pipeline \glsxtrshort{api} van \gls{huggingface}. Het is de basis voor verdere ontwikkeling en evaluatie van de komende integraties. Houd er rekening mee dat dit script een internetverbinding vereist om de \glsxtrshort{api} te kunnen gebruiken.

\subsection{Transformer Bibliotheek: Low-Level API}

In dit script maken we gebruik van de Transformer-bibliotheek van \gls{huggingface} op een lower-level. Dit script verschilt enigszins van het eerste script, dat de high-level \glsxtrshort{api} via pipelines benutte. Door over te stappen naar een lower-level benadering, krijgen we beter inzicht in de interne werking van de \gls{huggingface} pipeline, wat de overgang naar \glsxtrlong{tflite} vergemakkelijkt.

Het \texttt{tf-main.py} script, zoals weergegeven in de codefragmenten \ref{lst:low-level-main-part1} en \ref{lst:low-level-main-part2}, voert \glsxtrshort{mlm} uit met behulp van de Transformer-bibliotheek, waarbij we direct interacteren met de model en tokenizer objecten. Dit biedt meer flexibiliteit en controle vergeleken met de high-level pipeline benadering.

\begin{listing} [ht]
    \begin{minted}[fontsize=\small]{python}
        # tf-main.py
        import logging
        import timeit
        
        import torch
        from optimum.bettertransformer import BetterTransformer
        from transformers import AutoTokenizer, AutoModelForMaskedLM
        
        from utils import preprocess_input, get_top_predictions, get_sentences
        
        # Set up logging
        logging.basicConfig(level=logging.INFO)
        
        def set_log_level(log_level):
            """Set the logging level."""
            logging.getLogger().setLevel(log_level)
        
        def load_model_and_tokenizer(model_name):
            """Load the model and tokenizer."""
            tokenizer = AutoTokenizer.from_pretrained(model_name)
            model = AutoModelForMaskedLM.from_pretrained(model_name)
            # Apply BetterTransformer to the model for improved performance
            model = BetterTransformer.transform(model)
            return model, tokenizer
    \end{minted}
    \caption[\texttt{tf-main.py} (deel 1)]{Het eerste deel van \texttt{tf-main.py} laadt de modellen en tokenizers op een lager niveau. Dit geeft meer controle over de modelparameters en laat verbeteringen toe door de integratie van \texttt{BetterTransformer}, wat zorgt voor verhoogde prestaties. Hoewel deze optimalisaties de modellen zouden moeten verbeteren, zullen we later in de evaluatie zien dat dit weinig verschil maakt. Dit komt waarschijnlijk omdat de \texttt{pipeline} \glsxtrshort{api} van \gls{huggingface} deze optimalisaties al intern gebruikt.}
    \label{lst:low-level-main-part1}
\end{listing}

Het belangrijkste verschil met het high-level script is de directe interactie met de modellen en tokenizers, waardoor we toegang krijgen tot de interne structuren van de modellen. Hierdoor kunnen we verbeteringen aanbrengen met \texttt{BetterTransformer}, wat de prestaties verbetert door het model te optimaliseren. Echter, zoals later zal blijken in de evaluatiefase, lijkt de verbetering in prestaties beperkt te zijn, mogelijk doordat de high-level \texttt{pipeline} \glsxtrshort{api} van \gls{huggingface} al soortgelijke optimalisaties toepast.

In tegenstelling tot het high-level script, waarbij de \texttt{pipeline} functie automatisch de voorspellingen afhandelt, gebruiken we in deel 2 van het \texttt{tf-main.py} script de functie \texttt{get\_top\_predictions} om handmatig de top voorspellingen te bepalen. Hierbij wordt gebruik gemaakt van \gls{softmax} om de waarschijnlijkheidsverdeling van de modeloutput te berekenen. Dit biedt ons meer inzicht en controle over het voorspelling proces.

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # tf-main.py
        def run_inference(model, tokenizer, input_sentence):
            """Run model inference on the input sentence."""
            inputs = tokenizer(input_sentence, return_tensors="pt")
            logging.debug(f"Input tokens: {inputs}")
            with torch.no_grad():
                outputs = model(**inputs)
            return outputs.logits
        
        def main(model_name):
            """Main function to process sentences with the specified model."""
            sentences = get_sentences()
            
            # Load the model and tokenizer
            model, tokenizer = load_model_and_tokenizer(model_name)
            logging.info(f"Model loaded: {model_name}")
            
            start_total_time = timeit.default_timer()
            for input_sentence in sentences:
                # Preprocess the input
                processed_sentence, mask_position = preprocess_input(input_sentence)
                logging.debug(f'Input sentence: {input_sentence}')
                logging.debug(f'Processed sentence: {processed_sentence}')
                
                # Run inference and get top predictions
                if mask_position is not None:
                    start_time = timeit.default_timer()
                    outputs = run_inference(model, tokenizer, processed_sentence)
                    elapsed_time = timeit.default_timer() - start_time
                    top_words = get_top_predictions(outputs, tokenizer.get_vocab(), mask_position)
                    logging.debug(f'Top predicted words: {top_words}')
                    
                    for word in top_words[:3]:  # Use top 3 predictions
                        output_sentence = processed_sentence.replace('<mask>', word)
                        logging.info(f'Output sentence: {output_sentence}')
                    
                    logging.info(f"Elapsed time: {elapsed_time:.3f} seconds\n")
                else:
                    logging.info('No mask token found in the input sentence.')
            
            total_time = timeit.default_timer() - start_total_time
            logging.info(f"Total elapsed time: {total_time:.3f} seconds")
            logging.info("Average time per sentence: {:.3f} seconds".format(total_time / len(sentences)))
    \end{minted}
    \caption[\texttt{tf-main.py} (deel 2)]{Het tweede deel van \texttt{tf-main.py} bevat \texttt{run\_inference}, waarbij elke zin wordt verwerkt met behulp van PyTorch tensors. Dit biedt meer controle over de uitvoer van het model en maakt gedetailleerde \texttt{logging} van voorspellingen en verwerkte zinnen mogelijk. Bovendien gebruiken we nu de functie \texttt{get\_top\_predictions} in combinatie met de softmax-functie om zelf de top voorspellingen te bepalen.}
    \label{lst:low-level-main-part2}
\end{listing}

Het script meet de tijd die nodig is om elke zin te verwerken, evenals de totale verwerkingstijd voor alle zinnen. Dit gebeurt op een vergelijkbare manier als in het high-level script, met behulp van de \texttt{timeit} module, maar nu met meer inzicht in de verwerkingstappen door direct gebruik van PyTorch.

Dit script is een essentieel onderdeel van de ontwikkeling omdat het de mogelijkheden van \gls{robbert}-modellen verder demonstreert, maar met meer controle en optimalisatie mogelijkheden vergeleken met de high-level pipeline. Daarbij bereidt het de weg voor de integratie met \glsxtrshort{tflite}. Net als het high-level script vereist dit script een internetverbinding voor het laden van de modellen en tokenizers.

Het uitvoerblok van het script \texttt{tf-main.py}, dat doorgaans wordt gestart met \texttt{if \_\_name\_\_ == '\_\_main\_\_':}, is weggelaten om herhaling te vermijden. Dit deel bevat de beschrijving van de scriptfunctie en roept de hoofdprogramma's aan, zoals eerder beschreven in het high-level script.

\subsection{Conversie naar TFLite}

Het doel van deze stap is om de \gls{robbert}-modellen te converteren naar het \glsxtrshort{tflite}-formaat met behulp van \gls{huggingface} Optimum. Deze conversie maakt het mogelijk om de modellen efficiënt te implementeren op mobiele apparaten. Bovendien biedt deze stap de mogelijkheid om gekwantiseerde modellen als output te genereren, wat de prestaties verder kan verbeteren.

In deze fase maken we gebruik van twee bashscripts om de conversieprocessen te automatiseren. Het eerste script \ref{lst:convert-tf-models}, \texttt{convert\_tf\_models.sh}, voert een eenvoudige conversie uit van de \gls{robbert}-modellen naar het \glsxtrshort{tflite}-formaat zonder kwantisering. Het tweede script \ref{lst:convert-tf-models-quantized}, \texttt{convert\_tf\_models\_quantized.sh}, voert een vergelijkbare conversie uit, maar past daarbij int8-dynamische kwantisering toe om de modelgrootte te verminderen en de inferentiesnelheid te verhogen.

\begin{listing}
    \begin{minted}[fontsize=\small]{bash}
        # convert_tf_models.sh
        #!/bin/bash
        
        # Array of models to convert
        models=(
        "DTAI-KULeuven/robbertje-1-gb-non-shuffled"
        "pdelobelle/robbert-v2-dutch-base"
        "DTAI-KULeuven/robbert-2022-dutch-base"
        "DTAI-KULeuven/robbert-2023-dutch-base"
        "DTAI-KULeuven/robbert-2023-dutch-large"
        )
        
        # Function to convert a model
        convert_model() {
            local model=$1
            local model_name=$(basename $model)
            echo "Converting $model"
            
            optimum-cli export tflite \
                -m $model \
                --task fill-mask \
                --sequence_length 128 \
                ${model_name}_tflite
            
            echo "Conversion complete for $model"
            echo "------------------------"
        }
        
        # Main execution
        echo "Starting model conversions..."
        for model in "${models[@]}"; do
            convert_model $model
        done
        
        echo "All conversions completed!"
    \end{minted}
    \caption[\texttt{convert\_tf\_models.sh}]{Het \texttt{convert\_tf\_models.sh} script voert de conversie van de \gls{robbert}-modellen uit naar het \glsxtrshort{tflite}-formaat zonder kwantisering. Het maakt gebruik van de \texttt{optimum-cli} en exporteert met een vaste sequentielengte van 128 tokens.}
    \label{lst:convert-tf-models}
\end{listing}

\begin{listing}
    \begin{minted}[fontsize=\small]{bash}
        # convert_tf_models_quantized.sh
        #!/bin/bash
        
        # Array of models to convert
        models=(
        "DTAI-KULeuven/robbertje-1-gb-non-shuffled"
        "pdelobelle/robbert-v2-dutch-base"
        "DTAI-KULeuven/robbert-2022-dutch-base"
        "DTAI-KULeuven/robbert-2023-dutch-base"
        "DTAI-KULeuven/robbert-2023-dutch-large"
        )
        
        # Function to convert a model
        convert_model() {
            local model=$1
            local model_name=$(basename $model)
            echo "Converting $model"
            
            optimum-cli export tflite \
                -m $model \
                --task fill-mask \
                --quantize int8-dynamic \
                --sequence_length 128 \
                ${model_name}_tflite_int8
            
            echo "Conversion complete for $model"
            echo "------------------------"
        }
        
        # Main execution
        echo "Starting model conversions..."
        
        for model in "${models[@]}"; do
            convert_model $model
        done
        
        echo "All conversions completed!"
    \end{minted}
    \caption[\texttt{convert\_tf\_models\_quantized.sh}]{Het \texttt{convert\_tf\_models\_quantized.sh} script voert de conversie van de \gls{robbert}-modellen uit naar het \glsxtrshort{tflite}-formaat met een vaste sequentielengte van 128 tokens alsook int8-dynamische kwantisering. Deze stap vermindert de modelgrootte en kan de inferentiesnelheid verhogen.}
    \label{lst:convert-tf-models-quantized}
\end{listing}

De scripts maken gebruik van HuggingFace's \texttt{optimum-cli} om de modellen te exporteren. Ze verwerken een lijst van vooraf gedefinieerde \gls{robbert}-modellen, zoals gespecificeerd in de modellenarray.

Door deze conversie krijgen we een veelzijdige set modellen die kunnen worden geëvalueerd op zowel snelheid als nauwkeurigheid, afhankelijk van de behoeften van de toepassing en de beschikbaarheid van hardwarebronnen.

\subsection{Implementatie van TFLite Model in Python}

Deze sectie beschrijft de implementatie van de geconverteerde \glsxtrshort{tflite}-modellen in een Python-script dat de \glsxtrshort{tflite} interpreter \glsxtrshort{api} gebruikt. Het script voert inferenties uit met deze modellen en maakt gebruik van een eigen tokenizer voor de verwerking van invoertekst.

Het \texttt{tflite-main.py} script, zoals weergegeven in de codefragmenten \ref{lst:tflite-main-part1}, \ref{lst:tflite-main-part2} en \ref{lst:tflite-main-part3}, demonstreert de werking van de \glsxtrshort{tflite} modellen. In tegenstelling tot het \texttt{tf-main.py} script, dat gebruikmaakt van de volledige Transformer-bibliotheek, biedt dit script een meer gelijkaardige benadering voor inferentie op mobiele apparaten.

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # tflite-main.py
        import json
        import logging
        import re
        import timeit
        
        import numpy as np
        import tensorflow as tf
        from tf_keras.src.utils import pad_sequences
        
        from utils import preprocess_input, get_top_predictions, get_sentences
        
        # Set up logging
        logging.basicConfig(level=logging.INFO)
        
        def set_log_level(log_level):
            """Set the logging level."""
            logging.getLogger().setLevel(log_level)
        
        def load_model(model_path):
            """Load and initialize the TFLite model."""
            interpreter = tf.lite.Interpreter(model_path=model_path)
            interpreter.allocate_tensors()
            signatures = interpreter.get_signature_list()
            logging.debug(f"Model signatures: {signatures}")
            return interpreter
        
        def tokenize(sentence, vocab):
            """Tokenize the input sentence using the provided vocabulary."""
            mask_token_id = vocab.get('<mask>', vocab.get('<unk>'))
            # Split the sentence on word boundaries and punctuation, but keep <mask> intact
            split_sentence = re.findall(r'<mask>|\w+|[^\w\s]', sentence, re.UNICODE)
            logging.debug(f'Split sentence: {split_sentence}')
            tokenized_sentence = []
            for token in split_sentence:
                if token == '<mask>':
                    tokenized_sentence.append(mask_token_id)
                elif re.match(r'[^\w\s]', token):
                    tokenized_sentence.append(vocab.get('Ġ', vocab.get('<unk>')))
                    tokenized_sentence.append(vocab.get(token, vocab.get('<unk>')))
                else:
                    tokenized_sentence.append(vocab.get('Ġ' + token, vocab.get('<unk>')))
            
            return tokenized_sentence
    \end{minted}
    \caption[\texttt{tflite-main.py} (deel 1)]{Het eerste deel van \texttt{tflite-main.py} laadt en initialiseert de \glsxtrshort{tflite}-modellen en implementeert een aangepaste tokenization methode. Deze methode maakt gebruik van een woordenschatbestand om invoerzinnen te tokeniseren, wat cruciaal is voor de compatibiliteit met de modellen.}
    \label{lst:tflite-main-part1}
\end{listing}

In het \texttt{tflite-main.py} script moet de tokenisatie handmatig worden uitgevoerd met de \texttt{tokenize} functie, in plaats van automatisch te worden afgehandeld door de Transformer-bibliotheek zoals in het \texttt{tf-main.py} script. Deze aanpak geeft meer controle over de manier waarop de invoerdata wordt geprepareerd voor inferentie. Daarom verschilt het script ook in de manier waarop data wordt voorbereid, inclusief de toevoeging van padding, start- en eindtokens, en de creatie van de \gls{attentionmask}.
Het \texttt{tflite-main.py} script verschilt verder van het \texttt{tf-main.py} script doordat het gebruikmaakt van de \glsxtrshort{tflite} specifieke stappen voor inferentie, waar de \glspl{tensor} manueel ingesteld worden. De functie \texttt{run\_inference} in \ref{lst:tflite-main-part2} voert het model uit via de \glsxtrshort{tflite} interpreter. Daarnaast is het belangrijk op te merken dat dit script geen internetverbinding vereist, aangezien de modellen al lokaal zijn opgeslagen na conversie.


\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # tflite-main.py
        def run_inference(interpreter, input_ids, attention_mask):
            """Run inference using the TFLite interpreter."""
            input_details = interpreter.get_input_details()
            output_details = interpreter.get_output_details()
            logging.debug(f"Input details: {input_details}")
            logging.debug(f"Output details: {output_details}")
            interpreter.set_tensor(input_details[0]['index'], attention_mask)
            interpreter.set_tensor(input_details[1]['index'], input_ids)
            interpreter.invoke()
            return interpreter.get_tensor(output_details[0]['index'])
    \end{minted}
    \caption[\texttt{tflite-main.py} (deel 2)]{Het tweede deel van \texttt{tflite-main.py} behandelt de uitvoering van inferentie met de \glsxtrshort{tflite} interpreter, waarbij de invoer en uitvoer handmatig worden ingesteld via de \glspl{tensor}.}
    \label{lst:tflite-main-part2}
\end{listing}

\begin{listing}
    \begin{minted}[fontsize=\small]{python}
        # tflite-main.py
        def main(model_dir=None):
            """Main function to process sentences with the specified TFLite model."""
            model_path = model_dir + '/model.tflite'
            vocab_path = model_dir + '/vocab.json'
            
            sentences = get_sentences()
            
            # Load vocabulary and model
            with open(vocab_path, 'r') as f:
                vocab = json.load(f)
            interpreter = load_model(model_path)
            logging.info(f"Model loaded: {model_path}")
            
            for input_sentence in sentences:
                # Preprocess and tokenize input
                processed_sentence, mask_position = preprocess_input(input_sentence)
                saved_processed_sentence = processed_sentence
                processed_sentence = tokenize(processed_sentence, vocab)
                
                # Prepare input for inference
                processed_sentence = [vocab['<s>']] + processed_sentence + [vocab['</s>']]
                processed_sentence = pad_sequences([processed_sentence], maxlen=128, dtype='int64', value=1, padding='post',
                truncating='post')
                input_ids = np.array(processed_sentence, dtype=np.int64)
                attention_mask = np.ones_like(input_ids)
                
                # Run inference and get top predictions
                if mask_position is not None:
                    output = run_inference(interpreter, input_ids, attention_mask)
                    top_words = get_top_predictions(output, vocab, mask_position)
                    
                    for word in top_words[:3]:  # Use top 3 predictions
                        output_sentence = saved_processed_sentence.replace('<mask>', word)
                        logging.info(f'Output sentence: {output_sentence}')
                        
                else:
                    logging.info('No mask token found in the input sentence.')
    \end{minted}
    \caption[\texttt{tflite-main.py} (deel 3)]{Het derde deel van \texttt{tflite-main.py} voert de hoofdtoepassing uit, waarbij elke zin wordt verwerkt met behulp van de \glsxtrshort{tflite} interpreter. Het script legt de nadruk op het voorbereiden van de input met behulp van de aangepaste tokenizer en de \glsxtrshort{tflite} specifieke inferentie stappen. Debug- en tijdlogging is verwijderd voor compactheid.}
    \label{lst:tflite-main-part3}
\end{listing}

Dit script biedt een alternatieve benadering voor het uitvoeren van inferenties die dichter aanliggen bij de werking van dat op Android. Het uitvoerblok van het script \texttt{tflite-main.py}, dat normaal begint met \texttt{if \_\_name\_\_ == '\_\_main\_\_':}, is weggelaten om herhaling te vermijden. Dit deel roept de hoofdprogramma's aan en zorgt ervoor dat alle benodigde modellen worden verwerkt, vergelijkbaar met het vorige script.
Debug- en tijdlogging is verwijderd voor \ref{lst:tflite-main-part3} om de code compacter te maken en te laten passen in het document zonder verlies van overzicht.

\section{Android Implementatie}

Alle code en functionaliteit die in deze sectie wordt besproken, is beschikbaar in de volgende GitHub-repository: \url{https://github.com/Chmpy/RobBERT_local_Android_example}. Deze repository bevat de Android-applicatie in zijn geheel voor het integreren van \gls{robbert}.

\subsection{Compatibiliteit en Instellingen}

In deze sectie bespreken we de specificaties van de Android-applicatie om compatibiliteit en consistentie met de eerder genoemde scripts en modellen te waarborgen. Het manifestbestand (\ref{lst:android-manifest}) bevat de nodige instellingen om de applicatie correct te laten functioneren op mobiele apparaten. Hierin worden activiteiten zoals \texttt{ProcessTextActivity}, de bijhorende \texttt{intents} en \gls{services} zoals \texttt{SpeedTestService} gedefinieerd, wat essentieel is voor de verwerking en functionaliteit van de applicatie. 
Daarnaast zorgt de \texttt{libs.versions.toml} file (\ref{lst:libs-versions}) voor het beheren van consistente versies van bibliotheken in de gehele applicatie. Deze bevat de versie-informatie voor onze bibliotheken zoals \texttt{Kotlin}, \texttt{TensorFlow}, en \texttt{Compose}.
Om de modellen lokaal te gebruiken, plaatsen we handmatig een van de geconverteerde en gekwantiseerde modellen en hun vocabulaire in de \texttt{assets} map van de Android-applicatie, zoals weergegeven in \autoref{fig:androidassets}.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.8\linewidth]{androidassets}
    \caption[Assetmap met model en vocabulaire]{De \texttt{assets}-map bevat het geconverteerde en gekwantiseerde model en het bijbehorende vocabulairebestand, die essentieel zijn voor inferentie op het apparaat.}
    \label{fig:androidassets}
\end{figure}


\begin{listing}
    \begin{minted}[fontsize=\small]{xml}
        <!-- AndroidManifest.xml -->
        <activity
        android:name=".ProcessTextActivity"
        android:exported="true"
        android:label="@string/process_text_action_name"
        android:theme="@android:style/Theme.NoDisplay">
        <intent-filter>
        <action android:name="android.intent.action.PROCESS_TEXT" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
        </intent-filter>
        <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="http" />
        <data android:scheme="https" />
        </intent-filter>
        </activity>
        <service android:name=".SpeedTestService" />
        <activity
        android:name=".MainActivity"
        android:exported="true"
        android:theme="@style/Theme.RobBERT_local_example">
        <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    \end{minted}
    \caption[AndroidManifest.xml]{Instellingen in het \texttt{AndroidManifest.xml} bestand die de activiteiten en \gls{services} binnen de applicatie definiëren, waaronder \texttt{ProcessTextActivity} en \texttt{SpeedTestService}.}
    \label{lst:android-manifest}
\end{listing}

\begin{listing}
    \begin{minted}[fontsize=\small]{toml}
        # libs.versions.toml
        agp = "8.4.0"
        kotlin = "1.9.0"
        coreKtx = "1.13.1"
        lifecycleRuntimeKtx = "2.8.0"
        activityCompose = "1.9.0"
        composeBom = "2023.08.00"
        tensorflow = "2.13.0"
        gson = "2.10.1"
    \end{minted}
    \caption[\texttt{libs.versions.toml}]{De \texttt{libs.versions.toml} file bevat de versie-informatie voor de verschillende gebruikte bibliotheken binnen de Android-applicatie, waarmee compatibiliteit en consistentie worden gegarandeerd.}
    \label{lst:libs-versions}
\end{listing}

\subsection{Utility Object}

In dit deel behandelen we de utility-functies die essentieel zijn voor het consistent gedrag van de \glsxtrshort{tflite}-modellen binnen de Android-applicatie. Deze functies, gedefinieerd in het \texttt{Utils.kt} bestand, zorgen voor de verwerking van invoertekst en de voorbereiding van gegevens voor inferentie.

\subsubsection{Preprocessing van Invoer}

De functie \texttt{preprocessInput} (\ref{lst:preprocess-input}) is verantwoordelijk voor het maskeren van de woorden "die" of "dat" in de invoerzin en retourneert de gemaskeerde zin samen met de positie van de maskering.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Mask "die" or "dat" in the input sentence.
        *
        * @param sentence The input sentence.
        * @return A pair containing the preprocessed sentence and the position of the mask, or null if no mask was applied.
        */
        fun preprocessInput(sentence: String): Pair<String, Int?> {
            Log.d(TAG, "Preprocessing input: $sentence")
            val words = sentence.split(" ")
            var maskPosition: Int? = null
            val processedWords = words.mapIndexed { index, word ->
                if (word.lowercase() in listOf("die", "dat") && maskPosition == null) {
                    maskPosition = index
                    "<mask>"
                } else {
                    word
                }
            }
            val result = Pair(processedWords.joinToString(" "), maskPosition)
            Log.d(TAG, "Preprocessed result: $result")
            return result
        }
    \end{minted}
    \caption[\texttt{preprocessInput}]{De \texttt{preprocessInput} functie maskeert de woorden "die" of "dat" in de invoerzin en geeft de gemaskeerde zin en de positie van het masker terug.}
    \label{lst:preprocess-input}
\end{listing}

\subsubsection{Vocabulaire Laden}

De functie \texttt{loadVocabulary} (\ref{lst:load-vocabulary}) laadt het vocabulaire in vanuit een JSON-bestand in de \texttt{assets} map. Het retourneert een map waarin de sleutels de woorden zijn en de waarden de bijbehorende indices.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Load vocabulary from a JSON file located in the assets folder.
        *
        * @param context The context from which to access the assets.
        * @param filename The name of the JSON file containing the vocabulary.
        * @return A map representing the vocabulary, where keys are words and values are their corresponding indices.
        */
        fun loadVocabulary(context: Context, filename: String): Map<String, Int> {
            Log.d(TAG, "Loading vocabulary from $filename")
            val json = context.assets.open(filename).bufferedReader().use { it.readText() }
            val vocab: Map<String, Int> =
            Gson().fromJson(json, object : TypeToken<Map<String, Int>>() {}.type)
            Log.d(TAG, "Vocabulary loaded. Size: ${vocab.size}")
            return vocab
        }
    \end{minted}
    \caption[\texttt{loadVocabulary}]{De \texttt{loadVocabulary} functie laadt het vocabulaire vanuit een JSON-bestand in de \texttt{assets} map en retourneert een map van woorden en hun indices.}
    \label{lst:load-vocabulary}
\end{listing}

\subsubsection{Tokenisatie}

De \texttt{tokenize} functie (\ref{lst:tokenize}) converteert een zin naar een lijst van tokens op basis van het vocabulaire. Dit proces is essentieel voor het voorbereiden van de gegevens voor modelinference, omdat het de invoer omzet in een formaat dat het model kan verwerken.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Tokenize a sentence using the provided vocabulary.
        *
        * @param sentence The input sentence to tokenize.
        * @param vocab The vocabulary map to use for tokenization.
        * @return A list of integers representing the tokenized sentence.
        */
        fun tokenize(sentence: String, vocab: Map<String, Int>): List<Int> {
            Log.d(TAG, "Tokenizing sentence: $sentence")
            val maskTokenId = vocab["<mask>"] ?: vocab["<unk>"] ?: 0
            val pattern = Regex("<mask>|\\w+|[^\\w\\s]")
            val splitSentence = pattern.findAll(sentence).map { it.value }.toList()
            Log.d(TAG, "Split sentence: $splitSentence")
            val tokenizedSentence = mutableListOf<Int>()
            for (token in splitSentence) {
                when {
                    token == "<mask>" -> tokenizedSentence.add(maskTokenId)
                    token.matches(Regex("[^\\w\\s]")) -> {
                        (vocab["Ġ"] ?: vocab["<unk>"])?.let { tokenizedSentence.add(it) }
                        (vocab[token] ?: vocab["<unk>"])?.let { tokenizedSentence.add(it) }
                    }
                    else -> {
                        (vocab["Ġ$token"] ?: vocab["<unk>"])?.let { tokenizedSentence.add(it) }
                    }
                }
            }
            Log.d(TAG, "Tokenized sentence: $tokenizedSentence")
            return tokenizedSentence
        }
    \end{minted}
    \caption[\texttt{tokenize}]{De \texttt{tokenize} functie zet een zin om in een lijst van tokens op basis van het opgegeven vocabulaire.}
    \label{lst:tokenize}
\end{listing}

\subsubsection{Aanmaken van Attention Mask}

De functie \texttt{createAttentionMask} (\ref{lst:create-attention-mask}) genereert een \gls{attentionmask} voor een gegeven aantal tokens, waarbij alle waarden worden ingesteld op 1 om de volledige context te prioritiseren.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Create an attention mask for a given number of tokens.
        *
        * @param tokenCount The number of tokens for which to create the attention mask.
        * @return A long array representing the attention mask, with all values set to 1.
        */
        fun createAttentionMask(tokenCount: Int): LongArray {
            Log.d(TAG, "Creating attention mask of length $tokenCount")
            return LongArray(tokenCount) { 1L }
        }
    \end{minted}
    \caption[\texttt{createAttentionMask}]{De \texttt{createAttentionMask} functie creëert een aandachtmasker voor een gespecificeerd aantal tokens, waarbij alle waarden worden ingesteld op 1.}
    \label{lst:create-attention-mask}
\end{listing}

\subsubsection{Padding en Truncatie}

De functie \texttt{padOrTruncate} (\ref{lst:pad-or-truncate}) vult of snijdt een lijst van token-ID's af tot een gespecificeerde maximale lengte. Dit zorgt ervoor dat alle zinnen een uniforme lengte hebben voor verwerking door het model.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Pad or truncate a list of token IDs to a specified maximum length.
        *
        * @param ids The list of token IDs to pad or truncate.
        * @param maxLength The maximum length of the resulting list.
        * @param padToken The token ID to use for padding.
        * @return A list of token IDs padded or truncated to the specified length.
        */
        fun padOrTruncate(ids: List<Int>, maxLength: Int, padToken: Int): List<Int> {
            val result = if (ids.size > maxLength) {
                ids.take(maxLength)
            } else {
                ids + List(maxLength - ids.size) { padToken }
            }
            Log.d(TAG, "Padding/truncating complete. New length: ${result.size}")
            return result
        }
    \end{minted}
    \caption[\texttt{padOrTruncate}]{De \texttt{padOrTruncate} functie past padding of truncatie toe op een lijst van token-ID's, zodat deze overeenkomt met een gespecificeerde maximale lengte.}
    \label{lst:pad-or-truncate}
\end{listing}

\subsubsection{Softmax}

De \texttt{softmax} functie (\ref{lst:softmax}) wordt gebruikt om de \glspl{logit} om te zetten in waarschijnlijkheden. Het is een belangrijke stap in het verkrijgen van de waarschijnlijkheidsverdeling over de mogelijke voorspellingen, wat essentieel is voor het identificeren van de meest waarschijnlijke voorspelling voor de gemaskeerde token.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Apply softmax to a float array.
        *
        * @param input The float array.
        * @return A float array representing the probabilities after applying softmax.
        */
        private fun softmax(input: FloatArray): FloatArray {
            val max = input.maxOrNull() ?: 0f
            val exp = input.map { exp((it - max).toDouble()).toFloat() }
            val sum = exp.sum()
            return exp.map { it / sum }.toFloatArray()
        }
    \end{minted}
    \caption[\texttt{softmax}]{De \texttt{softmax} functie zet logits om in waarschijnlijkheden.}
    \label{lst:softmax}
\end{listing}

\subsubsection{Top-K Indices}

De \texttt{topKIndices} functie (\ref{lst:top-k-indices}) identificeert de indices van de top-\(k\) voorspellingen op basis van de waarschijnlijkheden. Deze functie sorteert de waarschijnlijkheden op aflopende volgorde en selecteert de indices van de hoogste waarden, dit geeft ons de meest relevante voorspellingen.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Get the top-k indices of a float array.
        *
        * @param x The float array.
        * @param k The number of top indices to retrieve.
        * @return An array of the top-k indices.
        */
        private fun topKIndices(x: FloatArray, k: Int): IntArray {
            return x.withIndex()
            .sortedByDescending { it.value }
            .take(k)
            .map { it.index }
            .toIntArray()
        }
    \end{minted}
    \caption[\texttt{topKIndices}]{De \texttt{topKIndices} functie identificeert de indices van de top-\(k\) voorspellingen op basis van de gesorteerde waarschijnlijkheden.}
    \label{lst:top-k-indices}
\end{listing}

\subsubsection{Top-K Voorspellingen}

De functie \texttt{getTopPredictions} (\ref{lst:get-top-predictions}) berekent de top-\(k\) voorspellingen voor het gemaskeerde token door de logit-uitvoer te verwerken. Hierbij worden de \texttt{softmax} (\ref{lst:softmax}) en \texttt{topKIndices} (\ref{lst:top-k-indices}) functies gebruikt om de waarschijnlijkheden te berekenen en de beste voorspellingen te selecteren.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // Utils.kt
        /**
        * Get top-k predictions for the masked token.
        *
        * @param logits The model output logits as a FloatBuffer.
        * @param vocab The vocabulary map.
        * @param maskPosition The position of the mask token, or null if no mask is present.
        * @param topK The number of top predictions to return.
        * @return A list of the top-k predicted words.
        */
        fun getTopPredictions(
        logits: FloatBuffer,
        vocab: Map<String, Int>,
        maskPosition: Int? = null,
        topK: Int = 5
        ): List<String> {
            
            Log.d(TAG, "Getting top predictions for mask position: $maskPosition")
            
            if (maskPosition == null) {
                Log.e(TAG, "Mask position is null. Cannot get predictions.")
                return emptyList()
            }
            
            Log.d(TAG, "Logits shape: ${logits.capacity()}")
            
            // Divide the logits into vocab.size chunks
            val dividedLogits = logits.array().toList().chunked(vocab.size)
            
            // Get logits for the mask token
            val maskTokenLogits = dividedLogits[maskPosition + 1].toFloatArray()
            
            // Apply softmax
            val probs = softmax(maskTokenLogits)
           
            // Get top-k indices
            val topIndices = topKIndices(probs, topK)     
            Log.d(TAG, "Top $topK indices: ${topIndices.contentToString()}")
            
            // Convert indices to words
            val predictions = topIndices.map { index ->
                vocab.entries.find { it.value == index }?.key ?: "<unk>"
            }
            
            Log.d(TAG, "Top $topK predictions: $predictions")
            return predictions
            }
        \end{minted}
    \caption[\texttt{getTopPredictions}]{De \texttt{getTopPredictions} functie berekent de top-\(k\) voorspellingen voor de gemaskeerde token door de \gls{logit}-uitvoer te verwerken en \gls{softmax} toe te passen om de waarschijnlijkheden te berekenen.}
    \label{lst:get-top-predictions}
\end{listing}

\subsubsection{Conclusie}
Al deze functies zijn noodzakelijk om effectief het gedrag van de Python-scripts te repliceren binnen de Android-omgeving. Ze zorgen ervoor dat de invoer correct wordt verwerkt en geformatteerd voor inferentie, vergelijkbaar met hoe dit in Python wordt gedaan.

\subsection{Ontwikkeling van de Android-applicatie}

In deze sectie beschrijven we de ontwikkeling van de Android-applicatie, met de nadruk op de integratie van het \glsxtrshort{tflite}-model. De eerder ontwikkelde Python-code wordt aangepast voor gebruik op Android. We maken gebruik van de \glsxtrlong{tflite} Android-bibliotheek om het model te integreren en uit te voeren binnen de \texttt{MainActivity}. Het utility-object, besproken in de vorige sectie, speelt een cruciale rol bij het bieden van consistente functionaliteit die overeenkomt met de Python-scripts.

De \texttt{MainActivity} (\ref{lst:main-activity-part1}, \ref{lst:main-activity-part2} en \ref{lst:main-activity-part3}) fungeert als het centrale onderdeel van de applicatie, waar de inferentie met het \glsxtrshort{tflite}-model wordt uitgevoerd. Binnen de \texttt{MainActivity} worden de \texttt{TFLite}-modellen geladen en wordt het vocabulaire ingeladen via \texttt{loadVocabulary} (\ref{lst:load-vocabulary}). De activiteit is verantwoordelijk voor het voorbereiden van de invoer, uitvoeren van de model-inferentie en het verwerken van de resultaten, net zoals in de Python-scripts. Daarnaast verzorgt de \texttt{MainActivity} de integratie van de gebruikersinterface waarin gebruikers zinnen kunnen invoeren voor inferentie, en biedt het ook de mogelijkheid om snelheidsmetingen uit te voeren met de \texttt{SpeedTestService}.

\begin{listing}[ht]
    \begin{minted}[fontsize=\small]{kotlin}
        // MainActivity.kt
        class MainActivity : ComponentActivity() {
            private lateinit var tflite: Interpreter
            private lateinit var vocab: Map<String, Int>
            private val MAX_SEQUENCE_LENGTH = 128
            
            override fun onCreate(savedInstanceState: Bundle?) {
                super.onCreate(savedInstanceState)
                
                // Load the TFLite model and vocabulary
                tflite = Interpreter(loadModelFile("model.tflite"))
                vocab = Utils.loadVocabulary(this, "vocab.json")
                
                setContent {
                    MaterialTheme {
                        MainScreen(::performInference, ::startSpeedTestService)
                    }
                }
            }
        \end{minted}
        \caption[\texttt{MainActivity.kt} (deel 1)]{Het eerste deel van \texttt{MainActivity.kt} initialiseert het \glsxtrshort{tflite}-model en laadt het vocabulairebestand, met behulp van functies zoals \texttt{loadVocabulary} uit het utility-object \ref{lst:load-vocabulary}.}
        \label{lst:main-activity-part1}
    \end{listing}
    
    \begin{listing}[ht]
        \begin{minted}[fontsize=\small]{kotlin}
            // MainActivity.kt
            private fun loadModelFile(modelName: String): MappedByteBuffer {
                val fileDescriptor = assets.openFd(modelName)
                val inputStream = FileInputStream(fileDescriptor.fileDescriptor)
                val fileChannel = inputStream.channel
                val startOffset = fileDescriptor.startOffset
                val declaredLength = fileDescriptor.declaredLength
                return fileChannel.map(FileChannel.MapMode.READ_ONLY, startOffset, declaredLength)
            }
            
            private fun performInference(input: String): String {
                val (processedInput, maskPosition) = Utils.preprocessInput(input)
                if (maskPosition == null) {
                    return "No mask token found in input"
                }
                var inputIds = Utils.tokenize(processedInput, vocab)
                inputIds = listOf(vocab["<s>"] ?: 0) + inputIds + listOf(vocab["</s>"] ?: 0)
                inputIds = Utils.padOrTruncate(inputIds, MAX_SEQUENCE_LENGTH, vocab["<pad>"] ?: 1)
                val attentionMask = Utils.createAttentionMask(inputIds.size)
                
                val inputBuffer = LongBuffer.wrap(inputIds.map { it.toLong() }.toLongArray())
                val maskBuffer = LongBuffer.wrap(attentionMask)
                val outputBuffer = FloatBuffer.allocate(MAX_SEQUENCE_LENGTH * vocab.size)
                
                try {
                    tflite.runForMultipleInputsOutputs(
                    arrayOf(maskBuffer, inputBuffer),
                    mapOf(0 to outputBuffer)
                    )
                } catch (e: Exception) {
                    return "Error during inference: ${e.message}"
                }
                
                var predictions = Utils.getTopPredictions(outputBuffer, vocab, maskPosition)
                predictions = predictions.map { it.replace("Ġ", "") }
                
                return predictions.joinToString(", ")
            }
        \end{minted}
        \caption[\texttt{MainActivity.kt} (deel 2)]{In dit tweede deel van \texttt{MainActivity.kt} worden functies zoals \texttt{performInference} gedefinieerd. Deze functie gebruikt methoden zoals \texttt{preprocessInput} en \texttt{tokenize} uit het utility-object om de invoerdata voor te bereiden.}
        \label{lst:main-activity-part2}
    \end{listing}
    
    \begin{listing}[ht]
        \begin{minted}[fontsize=\small]{kotlin}
            // MainActivity.kt
            private fun startSpeedTestService() {
                val intent = Intent(this, SpeedTestService::class.java)
                startService(intent)
            }
            @Composable
            fun MainScreen(onInference: (String) -> String, startSpeedTest: () -> Unit) {
                var input by remember { mutableStateOf("") }
                var result by remember { mutableStateOf("") }
                var newSentence by remember { mutableStateOf("") }
                
                fun getTopPrediction(input: String, prediction: String, maskPosition: Int): String {
                    if (maskPosition == 0) return ""
                    val words = input.split(" ")
                    val updatedWords = words.toMutableList()
                    updatedWords[maskPosition] = prediction.split(", ").first()
                    return updatedWords.joinToString(" ")
                }
                Column(modifier = Modifier.fillMaxSize().padding(16.dp), horizontalAlignment = Alignment.CenterHorizontally) {
                    TextField(
                    value = input,
                    onValueChange = { input = it },
                    label = { Text("Enter a sentence") },
                    modifier = Modifier.fillMaxWidth()
                    )
                    Button(onClick = {
                        result = onInference(input)
                        val (_, maskPosition) = Utils.preprocessInput(input)
                        newSentence = getTopPrediction(input, result, maskPosition ?: 0)
                    }) {
                        Text("Perform Inference")
                    }
                    Column(modifier = Modifier.fillMaxWidth(), horizontalAlignment = Alignment.Start) {
                        Text("Top predictions: $result")
                        Text("New sentence: $newSentence")
                    }
                    Button(onClick = {
                        startSpeedTest()
                    }) {
                        Text("Speed Test")
                    }
                }
            }
        }
    \end{minted}
    \caption[\texttt{MainActivity.kt} (deel 3)]{Het laatste deel van \texttt{MainActivity.kt} omvat de gebruikersinterface en de functie \texttt{MainScreen}, waarin gebruikers interacties zoals het starten van de inferentie en snelheids testen worden afgehandeld.}
    \label{lst:main-activity-part3}
\end{listing}

In dit voorbeeld zijn \texttt{logging} en \texttt{Spacers} verwijderd om de code compact te houden en gericht te laten zijn op de kernfunctionaliteit van de applicatie. De functies in \texttt{MainActivity} maken gebruik van de functionaliteit geboden door het utility-object, zoals \texttt{preprocessInput} (\ref{lst:preprocess-input}), \texttt{tokenize} (\ref{lst:tokenize}), en \texttt{getTopPredictions} (\ref{lst:get-top-predictions}), om een consistente en efficiënte uitvoering te garanderen.

\subsection{Integratie met Process Text Intent}

Als laatste binnen dit gedeelte behandelen we de implementatie van de \gls{processtextintent} binnen de Android-applicatie. Deze functionaliteit maakt het mogelijk om inferenties uit te voeren op tekst, zowel direct binnen de applicatie als via externe \texttt{intents}. De inferentieprocedure is gelijk aan die in de \texttt{MainActivity} (\ref{lst:main-activity-part2}), maar de gegevensverwerking verschilt enigszins vanwege het gebruik van \texttt{intents}. Deze aanpak verzekert dat de applicatie flexibel is en naadloos samenwerkt met andere apps op het Android-platform.

De \texttt{ProcessTextActivity} wordt geactiveerd wanneer een gebruiker tekst selecteert en de optie "Inference" kiest, zoals weergegeven in \autoref{fig:intent-example}. Dit stelt de gebruiker in staat om tekst uit andere applicaties te verwerken.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{intentexample}
    \caption[Gebruik van \texttt{ProcessTextActivity} via \texttt{intents}]{Voorbeeld van het gebruik van de \texttt{ProcessTextActivity} om tekstverwerking te initiëren via \texttt{intents} van Android. Dit proces stelt de applicatie in staat om interacties tussen verschillende apps soepel te beheren.}
    \label{fig:intent-example}
\end{figure}

\texttt{ProcessTextActivity} begint door te controleren of de intent van het type \texttt{ACTION\_PROCESS\_TEXT} is en of de tekst bewerkbaar is. Als aan deze voorwaarden is voldaan, wordt de geselecteerde tekst opgehaald en verwerkt (\ref{lst:process-text-check}). Deze aanpak zorgt ervoor dat de applicatie correct reageert op intents en alleen tekst verwerkt die kan worden aangepast.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // ProcessTextActivity.kt
        override fun onCreate(savedInstanceState: Bundle?) {
            super.onCreate(savedInstanceState)
            
            // Check if the intent is for processing text and is not read-only
            if (intent?.action == Intent.ACTION_PROCESS_TEXT &&
            intent.type == "text/plain" &&
            !intent.getBooleanExtra("readonly", false)
            ) {
                val text = intent.getStringExtra(Intent.EXTRA_PROCESS_TEXT)
                
                if (text == null) {
                    Toast.makeText(this, "No text to process", Toast.LENGTH_SHORT).show()
                    finish()
                    return
                }
                
                tflite = Interpreter(loadModelFile("model.tflite"))
                vocab = Utils.loadVocabulary(this, "vocab.json")
                
                handleSelectedText(text)
                
            } else {
                Toast.makeText(this, "Text cannot be modified", Toast.LENGTH_SHORT).show()
            }
            finish()
        }
    \end{minted}
    \caption[\texttt{ProcessTextActivity.kt} (deel 1)]{Het eerste deel van \texttt{ProcessTextActivity.kt} controleert of de intent van het juiste type is en haalt de geselecteerde tekst op voor verwerking.}
    \label{lst:process-text-check}
\end{listing}

Het verwerken van de geselecteerde tekst gebeurt in de functie \texttt{handleSelectedText} (\ref{lst:process-handle-selected-text}). Hierin wordt de \texttt{maskPosition} met behulp van \texttt{preprocessInput} (\ref{lst:preprocess-input}) opgehaald voor later gebruik en vervolgens wordt de inferentie uitgevoert.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // ProcessTextActivity.kt
        private fun handleSelectedText(selectedText: String) {
            
            // Preprocess the input text and get the mask position
            val (_, maskPosition) = Utils.preprocessInput(selectedText)
            
            if (maskPosition == null) {
                Toast.makeText(this, "No mask token found in input", Toast.LENGTH_SHORT).show()
                return
            }
            
            // Perform inference and get the top prediction, then replace the mask token with the prediction
            var feedback = performInference(selectedText).split(", ").first()
            feedback = feedback.replace("Ġ", "")
            val feedbackText = selectedText.split(" ").toMutableList().apply {
                set(maskPosition, feedback)
            }.joinToString(" ")
            
            // Set the result with the processed text
            val intent = Intent(Intent.EXTRA_PROCESS_TEXT)
            intent.putExtra(Intent.EXTRA_PROCESS_TEXT, feedbackText)
            setResult(RESULT_OK, intent)
        }
    \end{minted}
    \caption[\texttt{ProcessTextActivity.kt} (deel 2)]{Het tweede deel van \texttt{ProcessTextActivity.kt} verwerkt de geselecteerde tekst en past inferentie toe om de gemaskeerde woorden te voorspellen en te vervangen.}
    \label{lst:process-handle-selected-text}
\end{listing}

Om de code compact te houden en de kernfunctionaliteit te benadrukken, is \texttt{logging} verwijderd uit deze fragmenten (\ref{lst:process-text-check} en \ref{lst:process-handle-selected-text}). De \texttt{ProcessTextActivity} biedt een voorbeeld van hoe de applicatie externe input kan verwerken gebruik makend van \glsxtrshort{tflite}.

\subsection{Implementatie van de SpeedTestService}

De \texttt{SpeedTestService} (\ref{lst:speed-test-service}) is ontwikkeld om snelheidstesten uit te voeren op dezelfde manier als de Python-scripts. Deze \texttt{service} is verantwoordelijk voor het uitvoeren van inferenties op een reeks vooraf gedefinieerde zinnen, waardoor de prestaties van het \glsxtrshort{tflite}-model op het Android-platform worden geëvalueerd.

De service, zoals eerder vermeld, wordt gestart vanuit de \texttt{MainActivity} (\ref{lst:main-activity-part3}). De snelheidstest wordt uitgevoerd in een aparte thread, zodat de applicatie en het apparaat responsief blijft tijdens het uitvoeren van de batchverwerking.

\begin{listing}
    \begin{minted}[fontsize=\small]{kotlin}
        // SpeedTestService.kt
        class SpeedTestService : Service() {
            private lateinit var tflite: Interpreter
            private lateinit var vocab: Map<String, Int>
            private val MAX_SEQUENCE_LENGTH = 128
            
            override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
                // Load model and vocabulary
                tflite = Interpreter(loadModelFile("model.tflite"))
                vocab = Utils.loadVocabulary(this, "vocab.json")
                
                // Run the speed test in a separate thread
                Thread {
                    runSpeedTest()
                    stopSelf()  // Stop the service after completing the task
                }.start()
                
                return START_STICKY
            }
            
            private fun runSpeedTest() {
                val sentences = listOf(
                "Ik heb een vriend die altijd te laat komt.",
                "Ik weet die ik het kan.",
                // More sentences...
                )
                val totalTime = measureExecutionTime {
                    for (sentence in sentences) {
                        performInference(sentence)
                    }
                }
            }
            
            // Additional methods for loading the model, performing inference, etc.
            
            private inline fun measureExecutionTime(block: () -> Unit): Double {
                val elapsedTimeMillis = measureTimeMillis {
                    block()
                }
                return elapsedTimeMillis / 1000.0 // Convert milliseconds to seconds
            }
        }
    \end{minted}
    \caption[\texttt{SpeedTestService.kt}]{De \texttt{SpeedTestService} voert snelheidstesten uit voor het \glsxtrshort{tflite}-model, vergelijkbaar met de Python-scripts. De getoonde code is ingekort, maar functioneert op dezelfde manier als de activities in het project.}
    \label{lst:speed-test-service}
\end{listing}

In deze service worden zinnen verwerkt door middel van inferentie, de totale tijd en gemiddelde tijd die nodig is om alle zinnen te verwerken wordt geregistreerd. Deze aanpak biedt inzicht in de efficiëntie van het model op het Android.

\section{Evaluatie en Vergelijking van Inferentie}

In deze sectie evalueren we de prestaties van de verschillende implementaties van de \gls{robbert}-modellen, zoals eerder beschreven. We vergelijken de inferentietijden van de high-level \gls{huggingface} pipeline, de low-level Transformer-bibliotheek, en de \glsxtrshort{tflite}-modellen in zowel hun normale als gekwantiseerde vormen. Daarnaast evalueren we de prestaties van de Android-applicatie die gebruikmaakt van de gekwantiseerde modellen. Deze vergelijkingen helpen ons inzicht te krijgen in de effectiviteit en efficiëntie van elke benadering en maken het mogelijk om de beste aanpak te kiezen op basis van specifieke toepassingsbehoeften.

De evaluatie is uitgevoerd op de volgende apparaten:
\begin{itemize}
    \item \textbf{MacBookPro}: M3 Max-processor met 14 cores en 36 GB RAM.
    \item \textbf{Nothing Phone(2)}: Qualcomm Snapdragon 8+ Gen1 processor met 14GB RAM.
\end{itemize}

\subsection{High-Level Pipeline Evaluatie}

De eerste methode betreft het gebruik van de high-level \gls{huggingface} pipeline, die een gebruiksvriendelijke interface biedt voor het uitvoeren van modelinferences. Zoals weergegeven in \autoref{fig:speedtest-pipeline-graph}, toont de pipeline goede prestaties met redelijke inferentietijden voor alle modellen.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{speedtest_pipeline_graph.png}
    \caption[Inferentieprestatie met \gls{huggingface} pipeline]{Inferentieprestatie van modellen met de \gls{huggingface} pipeline. De horizontale as toont de verschillende modellen, terwijl de verticale as de totale inferentietijd (in seconden) en de gemiddelde inferentietijd per zin weergeeft, wat inzicht geeft in de efficiëntie van elk model.}
    \label{fig:speedtest-pipeline-graph}
\end{figure}

De grafiek laat zien dat de inferentietijden variëren per model, waarbij het model \texttt{robbert-2023-dutch-large} de hoogste totale tijd heeft. Dit komt doordat grotere modellen meer rekenkracht en tijd vereisen vanwege hun complexiteit. De pipeline biedt een goede balans tussen eenvoud en prestatie, waardoor het een aantrekkelijke keuze is voor veel toepassingen die geen controle over de inferentieprocessen vereisen.

\subsection{Low-Level Transformer API Evaluatie}

De tweede methode maakt gebruik van de low-level Transformer-bibliotheek, die meer controle over de modelverwerking mogelijk maakt. Zoals te zien in \autoref{fig:speedtest-transformer-graph}, zijn de prestaties van deze benadering vergelijkbaar met de pipeline, hoewel de inferentietijden iets hoger kunnen zijn.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{speedtest_transformer_graph.png}
    \caption[Inferentieprestatie met Transformer \glsxtrshort{api}]{Inferentieprestatie van modellen met de low-level Transformer \glsxtrshort{api}. De horizontale as geeft de verschillende modellen weer, terwijl de verticale as de totale en gemiddelde inferentietijd (in seconden) aangeeft, waardoor de effectiviteit van deze benadering zichtbaar wordt.}
    \label{fig:speedtest-transformer-graph}
\end{figure}

De extra controle die de low-level \glsxtrshort{api} biedt, kan nuttig zijn voor optimalisaties, maar introduceert ook extra overhead die kan leiden tot langere verwerkingstijden. Desondanks blijft de low-level benadering een waardevolle optie voor toepassingen die specifieke eisen stellen aan de verwerking van modeldata.

\subsection{TFLite Normale Conversie Evaluatie}

De \glsxtrshort{tflite} normale conversie wordt gepresenteerd in \autoref{fig:speedtest-tflite-normal-graph}, waarin de modellen zijn geconverteerd naar een formaat dat geschikt is voor gebruik op mobiele apparaten. Deze aanpak is bedoeld om inferenties mogelijk te maken met beperkte middelen.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{speedtest_tflite_normal_graph.png}
    \caption[Inferentieprestatie met \glsxtrshort{tflite} normale conversie]{Inferentieprestatie van modellen met de \glsxtrshort{tflite} normale conversie. De horizontale as toont de modellen, terwijl de verticale as de totale en gemiddelde inferentietijd (in seconden) toont, waarmee de prestaties van de onbewerkte \glsxtrshort{tflite} modellen worden geanalyseerd.}
    \label{fig:speedtest-tflite-normal-graph}
\end{figure}

Ons de mogelijkheid geeft om de modellen op apparaten te gebruiken met minder goede hardware zien we dat de inferentietijd langer kan zijn dan de Python-methoden door de aanpassingen van de modellen naar \glsxtrshort{tflite}.

\subsection{TFLite Gekwantisserde Conversie Evaluatie}

De gekwantiseerde conversie met \glsxtrshort{tflite}, weergegeven in \autoref{fig:speedtest-tflite-quantized-graph}, richt zich op het verkorten van inferentietijden door modellen te optimaliseren voor efficiënter gebruik van hardware.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{speedtest_tflite_quantized_graph.png}
    \caption[Inferentieprestatie met \glsxtrshort{tflite} gekwantiseerde conversie]{Inferentieprestatie van modellen met de \glsxtrshort{tflite} gekwantiseerde conversie. De horizontale as toont de modellen, terwijl de verticale as de totale en gemiddelde inferentietijd (in seconden) weergeeft, waarmee de verbeteringen door kwantisering worden geëvalueerd.}
    \label{fig:speedtest-tflite-quantized-graph}
\end{figure}

De gekwantiseerde modellen laten aanzienlijke verbeteringen zien in inferentietijden, vooral voor complexere modellen zoals \texttt{robbert-2023-dutch-large}. Kwantisering verlaagt de precisie van de berekeningen enigszins, wat de prestaties op hardware met beperkte middelen aanzienlijk verbetert.

\subsection{Android Gekwantisserde Implementatie Evaluatie}

De evaluatie van de Android-applicatie met gekwantiseerde modellen wordt getoond in \autoref{fig:speedtest-android-quantized-graph}. Deze implementatie maakt gebruik van de \glsxtrshort{tflite} gekwantiseerde modellen binnen een mobiele context.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{speedtest_android_quantized_graph.png}
    \caption[Inferentieprestatie van de Android-applicatie met gekwantiseerde modellen]{Inferentieprestatie van de Android-applicatie met gekwantiseerde modellen. De horizontale as geeft de modellen weer, terwijl de verticale as de totale en gemiddelde inferentietijd (in seconden) toont, wat inzicht biedt in de prestaties van de applicatie op mobiele apparaten.}
    \label{fig:speedtest-android-quantized-graph}
\end{figure}

De resultaten tonen aan dat, hoewel de inferentietijden op Android langer zijn dan in een desktopomgeving. Dit komt door de beperking aan middelen van een mobiel apparaat in vergelijking met een desktop, bieden de gekwantiseerde modellen nog steeds aanzienlijke verbeteringen ten opzichte van hun niet-gekwantiseerde tegenhangers.

\subsection{Conclusie en Aanbevelingen}

Uit de evaluaties blijkt dat er aanzienlijke verschillen zijn in de prestaties van de diverse implementaties. De high-level \gls{huggingface} pipeline biedt gebruiksgemak en goede prestaties, terwijl de low-level benadering meer controle biedt ten koste van iets langere inferentietijden. \glsxtrshort{tflite}-modellen, vooral wanneer gekwantiseerd, bieden aanzienlijke voordelen voor mobiele toepassingen met beperkte middelen.

De keuze voor een specifieke methode hangt af van de behoeften van de toepassing, zoals snelheid, nauwkeurigheid en resourcebeschikbaarheid. Toepassingen die eenvoud en snelle implementatie vereisen, profiteren waarschijnlijk van de pipelinebenadering, terwijl toepassingen die op mobiele apparaten moeten draaien baat kunnen hebben bij de \glsxtrshort{tflite}-optimalisaties.